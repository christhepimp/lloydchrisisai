# Training assets

## text-fiction-2-7.apk
Original Text Fiction interactive-fiction client (already signed in META-INF).

Upload or place the APK here as:
  `training/text-fiction-2-7.apk`

See `docs/TEXT_FICTION_TRAINING.md` for the full Lloyd play + learn API
(`/textfiction/observe`, `/suggest`, `/learn`) plus vision and audio endpoints.

The APK is ready to install (unknown sources) or re-sign with your keystore
for release distribution. Lloyd learns from story state over HTTP — no APK
recompile required for training.
